# UserApp
## Requirements for api/userapp ##
* Node.js
* MongoDB
* MongoDB Compass (optional)
## Installation Steps ##
* Clone repo
* Navigate to api/userapp
* Run npm install
* Create a .env file in the project root with database connection details
* Start MongoDB if running locally
* Run npm run watch
